#include <bits/stdc++.h>
using namespace std;

class Student_info
{
public:
    int id;
    string name;
    char section;
    double marks;

    Student_info(int s_id, string s_name, char s_section, double s_mark)
    {
        id = s_id;
        name = s_name;
        section = s_section;
        marks = s_mark;
    }
};

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    for(int i = 0; i<t; i++){
        Student_info obj1()
    }
    return 0;
}